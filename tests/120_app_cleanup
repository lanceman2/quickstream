#/bin/bash

# Seeing if it can run and with no memory leaks.
#
# Checks qsAppCreate() and qsAppDestroy() many times.


../bin/quickstream --stream
../bin/quickstream --stream --stream

exec ../bin/quickstream --stream --stream --stream

