#/bin/bash

set -e

# Seeing if it can run and with no memory leaks.
#
# Checks qsAppCreate() and qsAppDestroy() many times.


../bin/quickstream --stream # once
../bin/quickstream --stream --stream # twice

exec ../bin/quickstream --stream --stream --stream # three
